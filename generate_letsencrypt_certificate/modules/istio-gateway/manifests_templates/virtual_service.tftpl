apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: demoapp
  namespace: ${istio_ingress_namespace}
spec:
  hosts:
  - "*"
  gateways:
  - gateway-staging
  http:
    - route:
      - destination:
          host: myapp.${istio_ingress_namespace}.svc.cluster.local